# App screenshots

The **Key features** section on the landing page shows six captures from the
ArkSim app. Drop your real images in this folder, **keeping these exact
filenames** (the page references them directly):

| # | File | What it shows |
|---|------|----------------|
| 1 | `feature-1-import.png` | Convert workshop flipcharts, spreadsheets & photos into a simulation (structured prompts) |
| 2 | `feature-2-levers.png` | Adjust from baseline with levers — simplification, centralisation, automation, agents |
| 3 | `feature-3-visualise.png` | Auto-visualise the process with the auto-draw functions |
| 4 | `feature-4-simulate.gif` | **Animated GIF** — simulating the process |
| 5 | `feature-5-results.png` | Review the results |
| 6 | `feature-6-export.png` | Export results to Excel / JSON |

Notes:
- The files here now are **placeholders** — replace them with real captures.
- Recommended aspect ratio ~**16:10** (e.g. 1600×1000). They're shown with
  `object-cover`, so anything close works; matching the ratio avoids cropping.
- Keep each file **under 100 MB** (GitHub's push limit). A short, optimised GIF
  for #4 is best — if it's large, consider an MP4 hosted elsewhere and tell me.
- To use different filenames or add/remove items, edit the `screenshots` array
  in `src/app/page.tsx`.
